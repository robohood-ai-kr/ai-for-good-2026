# RoboHood Design System v1

RoboHood의 색상, 타이포그래피, 레이아웃, 공통 컴포넌트와 상호작용 기준을 정의한다. 제조업 중심과 소상공인 중심 서비스가 동일한 시각 언어를 사용하되, 각 서비스의 메뉴와 기능은 독립적으로 구성한다.

## 1. 디자인 방향

밝은 청회색 바탕에 흰 작업 카드를 배치하고, 파랑은 주요 행동과 선택 상태에 사용한다. 장식보다 현재 상태·담당자·다음 행동을 먼저 보여주는 한국어 데이터 플랫폼이다.

- 명확함: 현재 상태와 다음 행동을 한눈에 이해할 수 있게 한다.
- 일관성: 같은 의미의 색상·아이콘·컴포넌트는 모든 화면에서 같은 방식으로 사용한다.
- 가독성: 한글과 숫자를 충분한 크기와 대비로 표시한다.
- 절제: 얇은 테두리, 부드러운 모서리, 여백으로 정보 영역을 구분한다.
- 현장 중심: 모바일에서도 핵심 작업과 중단·복귀 경로를 쉽게 찾을 수 있게 한다.

## 2. 색상 토큰

화면별 임의 색상 대신 의미별 토큰을 사용한다. 기본 테마는 라이트 테마다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `action.primary` | `#2563EB` | 주요 버튼·링크·선택 테두리 |
| `action.hover` / `action.pressed` | `#1D4ED8` / `#1E40AF` | 주요 행동의 호버·눌림 |
| `surface.canvas` | `#F4F7FB` | 페이지 배경 |
| `surface.card` | `#FFFFFF` | 카드·입력·대화상자 |
| `surface.subtle` | `#F8FAFC` | 사이드바·표 머리글 |
| `surface.selected` | `#EFF6FF` | 선택 메뉴·행·지도 핀 상세 |
| `text.primary` | `#0F172A` | 제목·본문·주요 숫자 |
| `text.secondary` | `#475569` | 설명·메타데이터 |
| `text.hint` | `#64748B` | 흰 배경 위의 부차 안내·입력 예시 |
| `text.inverse` | `#FFFFFF` | 파란 주요 버튼 위의 글자 |
| `border.subtle` | `#D5DDE7` | 장식적 카드 테두리·구분선 |
| `border.control` | `#64748B` | 입력·선택 상자 등 조작 영역의 경계 |
| `focus.ring` | `#2563EB` | 키보드 초점 표시 |

| 상태 | 글자·아이콘 / 옅은 배경 | 표시 예시 |
|---|---|---|
| 안내·진행 | `#1E40AF` / `#EFF6FF` | 수집 중, 검수 중 |
| 성공 | `#166534` / `#F0FDF4` | 검수 승인, 교육 확인 |
| 주의 | `#92400E` / `#FFFBEB` | 보완 요청, 조건 확인 필요 |
| 오류·위험 | `#B91C1C` / `#FEF2F2` | 업로드 실패, 작업 중단 |
| 중립 | `#475569` / `#F4F7FB` | 초안, 배정 대기 |

대부분의 면적은 흰색·청회색으로 유지한다. 파랑은 주요 행동과 선택 상태에 집중하고, 녹색은 성공 상태에 사용한다. 상태는 반드시 문구·아이콘을 함께 표시한다. 일반 위치 핀은 파랑이며 빨간 핀은 실제 경고가 있을 때만 쓴다.

`border.subtle`은 입력 경계나 초점을 대신할 수 없다. `text.hint`는 흰 배경 전용이며, 청회색·선택 배경에는 `text.secondary`를 사용한다. 비활성 버튼은 중립색으로 표시하고 이유를 주변의 읽을 수 있는 문장으로 설명한다.

## 3. 한글과 정보 위계

서체는 `Pretendard GOV`, 대체 서체는 `Pretendard`, `Noto Sans KR`, `system-ui`, `sans-serif` 순이다. 서체를 불러오지 못해도 대체 서체로 읽기와 조작에 문제가 없어야 한다.

| 용도 | 데스크톱 / 모바일 | 굵기 |
|---|---|---|
| 페이지 제목 | 32px / 24px | 700 |
| 섹션 제목 | 24px / 22px | 700 |
| 카드 제목 | 19px / 19px | 700 |
| 본문·입력·버튼 | 17px / 17px | 400 / 버튼 700 |
| 표·메뉴 | 16px / 16px | 400 / 강조 700 |
| 보조 정보·배지 | 15px / 15px | 400 / 강조 700 |
| 주요 통계 | 32px / 28px | 700 |

- 줄 간격은 기본 1.5배. 글자를 좁혀 넣기보다 줄바꿈과 정보 우선순위로 조정한다.
- 위 크기는 기본 16px 환경의 기준이며 구현은 `rem`을 사용한다. 브라우저 기본 글자 크기와 확대를 막지 않는다.
- 한 페이지에 주 제목 하나를 두고 제목 계층을 유지한다. 긴 설명은 한 줄 폭 약 70자 이내로 제한한다.
- 숫자는 단위·집계 기간·기준을 함께 표시한다. `미집계`와 `0건`을 구분한다.
- 같은 기능·역할·상태에는 동일한 용어를 사용한다. 긴 메뉴 이름은 의미가 바뀌지 않는 범위에서 간결하게 쓴다.
- 행동은 ‘등록’, ‘검수 요청’, ‘보완 내용 확인’처럼 결과가 분명한 동사로 쓴다. 알 수 없는 약어만으로 안내하지 않는다.

## 4. 레이아웃·간격·반응형

좌측 탐색, 상단 도구 영역, 카드형 작업 영역을 기본으로 한다. 화면 크기와 콘텐츠 우선순위에 따라 열 수를 줄이고 모바일에서는 핵심 작업을 먼저 배치한다.

| 화면 너비 | 구성 | 바깥 여백 / 카드 간격 |
|---|---|---|
| 768px 미만 | 단일 열, 메뉴는 접이식, 현장 작업은 핵심 행동 우선 | 16px / 16px |
| 768–1023px | 8열 그리드, 필요 시 2열 카드, 접이식 메뉴 | 24px / 16px |
| 1024px 이상 | 12열 그리드, 좌측 240px 메뉴, 상단 64px 도구 영역 | 24px / 24px |
| 1280px 이상 | 충분한 본문 너비에서 4개 통계 카드·지도/목록 분할 | 24–32px / 24px |

- 본문 최대 너비는 1440px, 입력 폼은 약 800px로 제한한다. 240px 메뉴·64px 상단 높이는 기본값이며 글자 확대 시 늘어날 수 있다.
- 간격은 4·8·16·24·32·48px, 카드 안쪽 여백은 데스크톱 24px·모바일 16px를 쓴다.
- 모서리는 카드·대화상자 12px, 입력·버튼 8px, 배지는 pill 형태다. 과도한 둥근 카드와 큰 그림자를 피한다.
- 카드는 기본 얇은 테두리로 구분한다. 그림자는 떠 있는 메뉴·대화상자에만 약하게 사용한다.
- 현장 탐색은 넓은 화면에서 지도:목록을 약 5:7로 배치한다. 본문 폭이 960px 미만이면 목록 우선과 지도 전환을 제공한다.
- 통계 카드는 공간에 따라 4→2→1열로 줄인다. 모바일에서 넓은 표는 핵심 필드 카드로 바꾸거나 표 영역만 가로 스크롤한다.
- 모바일 주요 행동은 최소 높이 48px로 두고, 하단 고정 시 안전 여백과 본문 패딩을 확보해 내용을 가리지 않는다.

## 5. 공통 컴포넌트 규칙

| 요소 | 디자인·동작 기준 |
|---|---|
| 탐색·헤더 | 로고·작업 공간·허용된 역할과 현재 메뉴를 표시. 선택 메뉴는 옅은 파랑 배경과 진한 글자·표시선으로 구분 |
| 통계 카드 | 제목 → 숫자·단위 → 기간·설명의 순서. 필요한 지표만 표시하며 가상의 성장률·고용 실적을 넣지 않음 |
| 주요 버튼 | 한 작업 영역에서 주요 행동 하나를 강조. 보조 행동은 테두리 또는 텍스트 버튼. 위험 작업은 대상·영향을 확인 |
| 입력 폼 | 위쪽에 상시 라벨, 아래에 도움말·오류. 필수 여부를 글자로 표시하고 placeholder만으로 설명하지 않음 |
| 표·필터 | 검색·필터·결과 수를 표 위에 배치. 정렬 상태를 표시하고 선택·페이지 이동 시 필터를 유지. 숫자는 오른쪽 정렬 |
| 지도·목록 | 동일 현장 선택을 양쪽에 반영. 지도 없이 목록만으로도 탐색·요청 가능. 공개 허용 수준의 위치만 표시 |
| 상세·단계 | 현재 단계·담당자·다음 행동을 먼저 표시. 검수·이용 승인·지급처럼 다른 상태를 하나의 진행률로 합치지 않음 |
| 알림·배지 | 아이콘 + 상태 문구. 오류·미승인 사유는 토스트만 띄우지 않고 해당 작업 영역에 유지 |
| 빈 상태·로딩 | 미등록, 검색 결과 없음, 권한 없음, 실패를 구분. 재시도·초기화 등 상황에 맞는 다음 행동을 제시 |
| 모바일 수집 | 교육·허용·감독·보상 조건, 기록 중 여부와 중단 경로를 명확히 표시. 업로드와 검수 완료를 구분 |

## 6. 아이콘·이미지·데이터 표현

- 아이콘은 단순한 선형 스타일로 통일하고 같은 의미에 서로 다른 아이콘을 섞지 않는다.
- 본문 아이콘은 20–24px를 기본으로 한다. 상태·버튼 아이콘은 텍스트의 기준선과 시각적 중심을 맞춘다.
- 사진은 작업 대상과 상황을 이해하는 데 필요한 영역을 중심으로 배치한다. 동일한 카드 그룹에서는 비율과 모서리를 통일한다.
- 이미지 안에 제목·버튼·입력란을 포함시키지 않는다. 실제 조작 요소와 중요한 문구는 UI 요소로 구현한다.
- 의미 있는 이미지에는 대체 텍스트를 제공하고 순수 장식 이미지는 보조 기술에서 제외한다.
- 생성 이미지, 예시 데이터, 모의 상태는 실제 기록과 구분해 표시한다. 장식용 숫자를 실제 실적으로 보이게 하지 않는다.
- 차트는 색상 수를 제한하고 단위·기간·범례를 함께 표시한다. 표·요약 등 다른 방식으로도 내용을 이해할 수 있게 한다.

## 7. 접근성

아래 항목은 디자인·구현의 검증 목표이며 실제 화면에서 별도로 확인한다.

- 일반 텍스트는 최소 4.5:1, 큰 텍스트는 최소 3:1로 확인한다. 조작 식별에 필요한 경계·아이콘은 인접 색상과 최소 3:1을 확인한다.
- 키보드만으로 주요 과제를 완료할 수 있게 한다. 초점은 2px 파란 외곽선과 2px 여백을 기본으로 하며 가려지지 않아야 한다.
- 모달은 진입·복귀 초점을 관리하고, 입력 오류는 해당 필드 및 오류 요약과 연결한다. 아이콘만 있는 버튼에는 접근 가능한 이름을 제공한다.
- 제품 목표로 조작 영역 최소 44×44px, 현장 모바일 주요 버튼 높이 48px를 적용한다. 단순 텍스트 링크까지 무조건 같은 크기로 늘리지는 않는다.
- 글자 200% 확대에서 내용·행동이 잘리지 않게 하고, 320 CSS px 폭에서도 주요 흐름을 사용할 수 있게 한다. 지도·데이터 표의 가로 스크롤은 해당 영역 안으로 제한한다.
- 차트에는 범례·직접 라벨·수치 요약을 제공하고, 지도에는 동등한 목록 경로를 둔다. 동작 효과는 절제하고 reduced-motion 설정을 존중한다.
- 접근 불가와 교육·동의 미확인은 숨기거나 회색 처리만 하지 말고, 원인과 필요한 행동을 알려준다. UI 비활성화와 서버 권한 검증은 별개다.

## 8. 상호작용과 모션

- 버튼·입력·탭에는 기본, 호버, 초점, 눌림, 선택, 비활성 상태를 정의한다. 터치 화면에서 호버만으로 정보를 전달하지 않는다.
- 처리 중인 작업에는 로딩 상태와 결과 피드백을 제공한다. 중복 실행을 막고 실패 시 입력한 내용을 가능한 한 유지한다.
- 삭제·중단 등 영향이 큰 행동은 대상을 명확히 표시하고 필요한 경우 확인 단계를 제공한다.
- 화면 전환과 상태 변화는 짧고 절제된 모션으로 표현한다. 반복 점멸이나 작업을 방해하는 장식 애니메이션은 사용하지 않는다.
- 다크·고대비 테마는 별도 토큰과 검증을 거쳐 적용한다. 기존 색상을 단순 반전하지 않는다.

## 9. 디자인 검수 체크리스트

- 색상·서체·간격·모서리가 공통 토큰과 일치하는가?
- 페이지 제목, 현재 상태, 주요 행동이 명확한가?
- 긴 한글, 큰 숫자, 빈 값, 오류 메시지에서도 레이아웃이 유지되는가?
- 320·375·768·1024·1440px 너비에서 내용이나 조작 요소가 잘리지 않는가?
- 키보드 탐색, 초점 표시, 글자 확대, 색상 대비를 확인했는가?
- 로딩·빈 상태·실패·권한 없음·완료 상태를 구분했는가?
- 모바일 하단 고정 영역이 본문과 입력을 가리지 않는가?
- 이미지·아이콘·차트에 필요한 대체 설명이 있는가?
- 예시와 실제 데이터, 서로 다른 승인·진행 상태가 혼동되지 않는가?
